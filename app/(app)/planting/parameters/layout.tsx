import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Parâmetros - Plantio",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
