import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Fechamento - Plantio",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}

